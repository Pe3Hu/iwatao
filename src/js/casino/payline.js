//
class payline{
  constructor ( index ){
    this.offset = offset;
    this.const = {      
      index: index,
      a: cellSize * 2
    };
    this.array = {
      link: []
    };
    this.var = {
      shift: 0
    };

    this.init();
  }

  init(){
  }

  draw( offset ){

  }
}
